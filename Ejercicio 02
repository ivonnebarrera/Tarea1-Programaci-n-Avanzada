# Ejercicio 02
N = int(input("Ingrese el número del que desea conocer su tabla de multiplicar: "))
print(("La tabla de multiplicar del número %d que usted ingresó es: ") % (N))
for n in range(1, 11, 1):
  print(("%d x %d = %d")%(N,n,N*n))
