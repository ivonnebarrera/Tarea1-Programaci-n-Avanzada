# Ejercicio 07
a = int(input("Ingrese el primer número: "))
b = int(input("Ingrese el segundo número: "))
producto = a
suma = 0
if a < b:
  for i in range(a, b + 1, 1):
    suma = suma + i
    producto = producto * i
  print("La suma es: {} El producto es: {}" .format(suma, producto))
elif a > b:
  for i in range(b, a + 1, 1):
    suma = suma + i
    producto = producto * i
    print("La suma es: {} El producto es: {}" .format(suma, producto))
